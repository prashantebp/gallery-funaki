$('.js-splide-1').length &&
new Splide( '.js-splide-1', {
    // perPage: 5,
    perMove: 1,
    gap: "29px",
    autoWidth:true,
    pagination: false,

} ).mount();

$('.js-splide-2').length &&
new Splide( '.js-splide-2', {
    // perPage: 5,
    perMove: 1,
    gap: "18px",
    autoWidth:true,
    pagination: false,

} ).mount();